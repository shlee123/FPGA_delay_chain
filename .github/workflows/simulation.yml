name: Simulation

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  behavioral:
    name: Icarus X-startup behavioral simulation
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Bootstrap workflow
        if: hashFiles('sim/Makefile') == ''
        run: echo "Simulation files are not present on this revision."

      - name: Install Icarus Verilog
        if: hashFiles('sim/Makefile') != ''
        run: |
          sudo apt-get update
          sudo apt-get install --yes iverilog

      - name: Compile and run X-startup self-checking testbench
        if: hashFiles('sim/Makefile') != ''
        run: make -C sim SIMULATOR=iverilog sim

      - name: Check waveform output
        if: hashFiles('sim/Makefile') != ''
        run: test -s sim/build/iverilog/tb_ku115_delay_chain.vcd
